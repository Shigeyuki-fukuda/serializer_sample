# README

## バージョン

- Ruby : 2.6.5
- Rails : 6.0.3.4
- MongoDB : 4.2

## Dockerの起動停止

- Dockerの起動

```bash
$ docker-compose -f provisioning/docker-compose.yml up -d
```

- Dockerの停止

```bash
$ docker-compose -f provisioning/docker-compose.yml up -d
```
